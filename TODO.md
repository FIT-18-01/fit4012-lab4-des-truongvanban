# TODO - fit4012-lab4-des-truongvanban

- [x] Implement `des.cpp` đúng contract stdin/stdout (4 mode, multi-block, zero padding).
- [ ] Xóa toàn bộ placeholder `TODO_STUDENT` trong `README.md`, `report-1page.md`, và `tests/test_*.sh`.
- [ ] Tạo/ cập nhật ít nhất 1 file minh chứng trong `logs/` (ví dụ `run-output.txt`).
- [ ] Chạy `bash .github/scripts/check_submission.sh` để xác nhận pass CI.

